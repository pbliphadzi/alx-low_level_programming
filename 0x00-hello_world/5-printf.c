#include <stdio.h>

/**
 * main - Print a string "with proper grammer, but the outcome is a piece of art"
 * Description: use printf
 * Return: 0
 */

int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
